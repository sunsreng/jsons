# jsons
```json
{
  "scripts": {
    "server": "json-server --watch db.json --port 5000",
    "server:github": "json-server https://raw.githubusercontent.com/sunsreng/jsons/main/src/movies.json --port 5000",
  },
}
